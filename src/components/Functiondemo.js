// this line is a import a react this is not neccessry not import this line to work function 
// note:- not import a react component becaste this function base is not inherit to other one 
import React from "react";

function Functiondemo(props) {


    return (
        <h1>
            
            <h1>hello my name is {props.name}</h1>
        </h1>

    )

}

// this line is the export the function 

export default Functiondemo;

// sumary function base comnponents is a nothing but the javascript function this is same ways to the class reciving props and returant html(jsx)
// this is a function componets to create a functuion 

